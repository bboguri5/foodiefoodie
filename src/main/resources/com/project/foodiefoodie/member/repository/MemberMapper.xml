<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.foodiefoodie.member.repository.MemberMapper">

    <resultMap id="memberMap" type="com.project.foodiefoodie.member.domain.Member">
        <result column="nick_name" property="nickName" />
        <result column="phone_number" property="phoneNumber"/>
        <result column="regist_date" property="regDate"/>
        <result column="login_time" property="recentLoginTime"/>
    </resultMap>


    <select id="isDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM member
        <if test="type=='email'">
        WHERE email = #{value}
        </if>
        <if test="type=='nickName'">
        WHERE nick_name = #{value}
        </if>
    </select>


    <select id="findMember" resultMap="memberMap">
        SELECT *
        FROM member
        WHERE email = #{email}
    </select>


    <insert id="register">
        INSERT INTO member
        (email, password, nick_name, phone_number, birth, address, name, gender)
        VALUES
        (#{email}, #{password}, #{nickName}, #{phoneNumber}, #{birth}, #{address}, #{name}, #{gender})
    </insert>


    <delete id="deleteMember">
        DELETE FROM member
        WHERE email = #{email}
    </delete>


    <update id="modifyMember">
        UPDATE member
        SET password = #{password}, nick_name = #{nickName}, phone_number = #{phoneNumber}, birth = #{birth}
        , address = #{address}, name = #{name}, gender = #{gender}
        WHERE email = #{email}
    </update>


    <update id="authChange">
        UPDATE member
        SET auth = #{auth}
        WHERE email = #{email}
    </update>
    
    
    <insert id="saveAutoLoginValue">
        INSERT INTO auto_login
            ()
        VALUES
            ()
    </insert>
</mapper>


